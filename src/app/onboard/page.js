import OnboardClient from "./OnboardClient";

export const metadata = {
  title: "Join as Influencer",
  description:
    "Sign up as a local influencer and get discovered by brands. Create your profile, upload your photo, and start receiving collaboration opportunities in your city.",
};

export default function OnboardPage() {
  return <OnboardClient />;
}
